Binary Search 的关键词，sorted
或者是Array，find position or search element.

二分查找的关键在于，每一次比较大小后，要排除掉一半的candidates。
如何排除掉一半，就要根据数组有序这一个特性。

变种有一种是Rotated Array，比如lc的153和154题。
这种情况下，要利用好局部还是有序，收尾和中间的关系。找到每次比较后排除一半的方法。
如果数组内有重复，则不能完美log n，有可能会O n，在所有元素都相同这类极端情况下。

测试的时候，先用只有2个数字的来测试，看能不能得到结果。再用1个的。同时考虑到是否有越界问题。比如数组是[1,2] 找3. 最后lo会是2，这时候就不能call nums[lo]了。